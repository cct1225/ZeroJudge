#include<iostream>
using namespace std;
int main()
{
    int n;
    int m,d;
    int date;
    cin>>n;
    while(n--)
    {
        cin>>m>>d;
        if(m==1)
        {
            date=d;
        }
        if(m==2)
        {
            
        }
        if(m==3)
        if(m==4)
        if(m==5)
        if(m==6)
        if(m==7)
        if(m==8)
        if(m==9)
        if(m==10)
        if(m==11)
        else


    }
}
